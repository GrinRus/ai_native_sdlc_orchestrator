route_id: route.plan.default
step: planning
route_class: planner
risk_tier: medium
primary:
  adapter: none
  provider: openai
  model: planning-primary
fallback:
  - adapter: none
    provider: anthropic
    model: planning-fallback
required_adapter_capabilities: []
validation_profile_ref: validate.plan.default
retry_policy_ref: retry.transient.default
repair_policy_ref: repair.plan.default
constraints:
  timeout_sec: 300
  max_cost_usd: 4
  allow_scope_expansion: false
promotion_channel: stable
