route_id: route.eval.default
step: eval
route_class: eval
risk_tier: low
primary:
  adapter: none
  provider: openai
  model: judge-primary
fallback:
  - adapter: none
    provider: anthropic
    model: judge-fallback
required_adapter_capabilities: []
validation_profile_ref: validate.eval.default
retry_policy_ref: retry.transient.default
repair_policy_ref: repair.eval.default
constraints:
  timeout_sec: 300
  max_cost_usd: 5
  allow_scope_expansion: false
promotion_channel: stable
