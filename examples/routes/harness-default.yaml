route_id: route.harness.default
step: harness
route_class: harness
risk_tier: medium
primary:
  adapter: none
  provider: openai
  model: judge-primary
fallback:
  - adapter: none
    provider: anthropic
    model: judge-fallback
required_adapter_capabilities: []
validation_profile_ref: validate.harness.default
retry_policy_ref: retry.transient.default
repair_policy_ref: repair.harness.default
constraints:
  timeout_sec: 900
  max_cost_usd: 6
  allow_scope_expansion: false
promotion_channel: stable
