# AGENTS.md

`examples/routes` contains example route profiles.

## Rules
- Keep route concerns limited to adapter/provider/model selection and routing constraints.
- Do not mix wrapper or prompt-bundle content into route examples.
- Keep refs aligned with wrappers, prompt bundles, and policies used by the example project profiles.
