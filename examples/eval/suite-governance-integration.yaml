suite_id: suite.governance.integration
version: 1
subject_type: run
dataset_ref: dataset://governance-integration@2026-05-21T08:00:00Z
graders:
  - deterministic
thresholds:
  min_pass_rate: 1.0
  max_regressions_introduced: 0
blocking_rules:
  - any-critical-regression
  - missing-traces
baseline_policy:
  required: false
retry_policy:
  flaky_case_retries: 0
