# AGENTS.md

`examples/wrappers` contains example execution envelopes for runner sessions.

## Rules
- Keep wrapper concerns focused on tools, commands, included context, and verification framing.
- Keep wrapper refs aligned with prompt bundles and step classes.
- Avoid embedding route-selection logic here.
