provider_variant_id: open-code-primary
provider: open-code
coverage_tier: extended
primary_adapter: open-code
fallback_policy:
  mode: none
  allow_fallback: false
route_override_policy:
  strategy: pin-primary-only
  steps:
    - discovery
    - research
    - spec
    - planning
    - implement
    - review
    - qa
    - repair
    - eval
    - harness
